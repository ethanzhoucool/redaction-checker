# Install into a venv:  python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
# Also requires the tesseract OCR binary:  brew install tesseract
pillow
numpy
texture2ddecoder
pytesseract
pyyaml
